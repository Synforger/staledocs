# docs-check exceptions: one substring or glob per line.
# A reference flagged by docs-check.sh is suppressed when its path matches
# any line here. Use for intentional references to paths that do not exist
# in this repository (e.g. examples in prose, planned layouts whose root
# already exists). Keep this list short — every entry is a hole in the net.
#
# Example:
#   docs/planned-layout.md      # whole-file worth of future paths
#   some/example/path.py        # single intentional example

task version:bump   # workflows/README: "derive per project when needed" example
task release:cut    # same — intentionally hypothetical, not a stale reference
task install:core   # back-port task lives in root Taskfile.yml at template state only (= not in post-init core Taskfile, so docs-check at template state can't see it)
.tooling/versions.yaml  # README documents post-init layout; the file lives under _core/.tooling/ at template state and gets promoted to .tooling/ by `task init`
